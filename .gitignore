# --- Dependencies -----------------------------------------------------------
node_modules/
.pnpm-store/

# --- Build output -----------------------------------------------------------
.next/
out/
dist/
build/
apps/api/bin/
apps/api/tmp/
*.test
*.out

# --- Environment & secrets --------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/**/*.key

# --- Local infrastructure ---------------------------------------------------
.docker-data/
postgres-data/
minio-data/

# --- Tooling caches ---------------------------------------------------------
.turbo/
.cache/
coverage/
*.lcov
.vitest/
playwright-report/
test-results/
# Playwright's stored sign-in: a real Clerk session, in plain text. Never a
# commit, and the reason the signed-in E2E credentials live in the environment.
apps/web/playwright/.clerk/
.golangci-cache/
# TypeScript's incremental cache: rewritten on every typecheck, never source.
*.tsbuildinfo

# --- Agent & editor ---------------------------------------------------------
.atl/
.code-review-graph/
.claude/settings.local.json
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
